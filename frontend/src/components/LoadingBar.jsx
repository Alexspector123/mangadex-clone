import React from 'react';

const LoadingBar = () => {
  return (
    <div className="loading-bar-container">
      <div className="loading-bar-runner" />
    </div>
  );
};

export default LoadingBar;